apiVersion: v1
kind: Service

metadata:
  name: expense-frontend-service
  namespace: prod

spec:
  selector:
    app: expense-frontend

  ports:
  - port: 3000
    targetPort: 3000

  type: ClusterIP
